const prisma = require('../database/prisma/client')

/**
* Função que imitav classe em js
* @param model aluno prisma 
* @propertys    
    type: ['criar','buscar']
*/

const Aluno = (model) => {
    return model
}

module.exports = Aluno